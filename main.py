"""
Import the module functions
In the main file, import the module functions using the following line:

from list_math import *
Call the module functions
Now, call the functions of the module from the main file to calculate and print the sum, count, average, max, and min of a given list.
"""

from list_math import *

list_math1 = [1,6,4]
print(_count(list_math1))
print(_sum(list_math1))
print(_average(list_math1))
print(_max(list_math1))
print(_min(list_math1))
